// Importa o componente 'SimpleBar' do pacote 'simplebar-react'.
import SimpleBar from 'simplebar-react';
// Importa a função 'styled' do Material-UI para criar componentes estilizados.
import { styled } from '@mui/material/styles';

// Cria o componente estilizado 'Scrollbar' usando 'styled' e baseado no componente 'SimpleBar'.
export const Scrollbar = styled(SimpleBar)``;

